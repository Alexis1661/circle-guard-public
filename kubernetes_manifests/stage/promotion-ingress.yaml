apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: promotion-ingress
  namespace: circle-guard-stage
  labels:
    app: circle-guard-promotion
    project: circle-guard
    environment: stage
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: circle-guard-promotion.stage.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: promotion-service
                port:
                  number: 8080
