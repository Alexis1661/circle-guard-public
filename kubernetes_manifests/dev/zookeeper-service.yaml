apiVersion: v1
kind: Service
metadata:
  name: zookeeper
  namespace: circle-guard-dev
  labels:
    app: zookeeper
    project: circle-guard
spec:
  type: ClusterIP
  selector:
    app: zookeeper
  ports:
    - name: client
      port: 2181
      targetPort: 2181
