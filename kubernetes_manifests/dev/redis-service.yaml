apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: circle-guard-dev
  labels:
    app: redis
    project: circle-guard
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
