apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: circle-guard-dev
  labels:
    app: kafka
    project: circle-guard
spec:
  type: ClusterIP
  selector:
    app: kafka
  ports:
    - name: kafka
      port: 9092
      targetPort: 9092
