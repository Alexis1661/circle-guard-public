apiVersion: v1
kind: Service
metadata:
  name: form-service
  namespace: circle-guard-dev
  labels:
    app: circle-guard-form
    project: circle-guard
spec:
  type: ClusterIP
  selector:
    app: circle-guard-form
  ports:
    - name: http
      port: 8080
      targetPort: 8086
