apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gateway-ingress
  namespace: circle-guard-master
  labels:
    app: circle-guard-gateway
    project: circle-guard
    environment: master
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - circle-guard-gateway.circleguard.edu
      secretName: circle-guard-gateway-tls
  rules:
    - host: circle-guard-gateway.circleguard.edu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway-service
                port:
                  number: 8080
