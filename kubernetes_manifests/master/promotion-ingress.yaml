apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: promotion-ingress
  namespace: circle-guard-master
  labels:
    app: circle-guard-promotion
    project: circle-guard
    environment: master
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - circle-guard-promotion.circleguard.edu
      secretName: circle-guard-promotion-tls
  rules:
    - host: circle-guard-promotion.circleguard.edu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: promotion-service
                port:
                  number: 8080
